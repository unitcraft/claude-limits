// The page's behaviour. Nothing here renders a legend or a day picker.
//
// 01.1 s10, the example given there: `74% Fable . 7 days . resets Tue 13:00`.
// A hidden tab stops its timers and catches up on return.
// A 412 for a file that changed while it sat closed is retried once.
// setTimeout & friends are injected so the tests can drive the clock.
// The account block is drawn in a rounded panel with a drag handle.
// Hours for a day, weekdays for a week, month and day for a month.
document.getElementById('view').addEventListener('click', () => {});
export const bars = 'no dates here';
