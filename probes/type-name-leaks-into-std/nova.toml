[lib]
src = "src"

[package]
name = "type_name_leaks_into_std"
version = "0.0.0"
description = "Probe: a type name exported by one module changes the meaning of the same name inside std"
license = "MIT OR Apache-2.0"
