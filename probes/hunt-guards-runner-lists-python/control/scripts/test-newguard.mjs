// The same failing check, written as a suite instead of a python guard.
console.log('  FAIL the thing this check exists to catch is present');
console.log('\n0 passed, SOME FAILED');
process.exit(1);
