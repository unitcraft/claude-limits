module p1.a

export type Mode enum
    | Once
    | Help

export fn Mode @to_str() -> str =>
    match @self {
        Mode.Once => "once"
        Mode.Help => "help"
    }

export fn pick(help bool) -> Mode => if help { Mode.Help } else { Mode.Once }
