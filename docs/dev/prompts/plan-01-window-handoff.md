# Снимок окна плана 01 — на случай перезапуска

**Обновлено:** 2026-09-09 20:39. **Роль:** окно, ведущее `docs/plans/01-widget-on-nova.md`
и его подпланы. Имя сессии здесь НЕ пишется: имена окон меняются при перезапуске, роль — нет.

**СНИМОК ОДИН НА ОБА ДОМА, И ЭТО РЕШЕНИЕ.** Работа идёт в двух деревьях
(`claude-limits` и `nova-sdl`), а файл здесь один и покрывает оба — раздел 5 про
второй. **Второй снимок заводить НЕ НАДО**, как бы разумно ни выглядело держать
состояние пакета в пакете: две копии состояния расходятся на первой правке, и читать
будут ту, что устарела. За смену этот класс встретился десятком форм — от точки
входа в двух планах до списка библиотек в трёх местах.

Файл заводится один раз и дальше обновляется. Читать сверху вниз: сперва чем занят, потом
что мешает, потом чему научился — последнее дороже всего, потому что нигде больше не лежит.

---

## 1. Границы, поставленные владельцем

- **Работать только в рамках плана 01 и его подпланов.** Шаг наружу — с его слова.
  Разрешение на `nova-sdl` получено 2026-09-09 («создание пакета nova-sdl - ДА»).
- **Значения из `.credentials.json` НЕ читать и НЕ печатать** — ни командой, ни в примере.
  О файле сообщаются только путь, размер и время изменения.
- **Никаких `.nv` в корне** `claude-limits`; всё под `src/`. Каталога `bin/` нет и не будет
  (решение записано в `nova.toml`, страж `check-plan-trees.py` его держит).
- **Время в каждом сообщении — МЕСТНОЕ, ровно `date "+%H:%M"`.** Не UTC, не чужой штамп.
  Нарушал за смену дважды: сперва добавлял `-u`, потом приращивал чужие числа в уме.
- **Лимиты — ресурс.** Механику отдавать haiku по `/delegate`, в докладе строка
  «Модели агентов: …».

## 2. Состояние на 18:13, снято замером

| | |
|---|---|
| коммитов за смену | 71 в `claude-limits`, 4 в `nova-sdl` |
| карточек с вердиктом | **33 из 51**; остальные 18 разобраны ОДНИМ блоком по трём причинам |
| мягких критериев (без названной команды) | **13** (было 29) |
| тесты | `./nova.sh test src/` → `PASS: 23 FAIL: 0`; `node scripts/check-web.mjs` → 220 тестов + 14 проверок |

**Закрыто по существу:** T1.4, T1.6 целиком; T1.1/T1.2/T1.3 перепрогнаны мной (были чужой
записью); T1.5 — четыре условия из шести; T2.9 — три из пяти; дыра `bind` (принимался
`0.0.0.0`, ПДн ушли бы в сеть); 13 сторожей перестали судить чужое дерево, 14-й это держит.

## 3. Три упора, все чужие

1. **Реестр №1033** — на этой машине не линкуется ни один пакет с HTTP-клиентом.
   Держит: `usage/fetch.nv`, `--once` (T1.7 проверено — НЕ написан), поллеры, три пункта
   T2.13, и через них T1.9 и T0.6 (обе невыполнимы арифметически). **Держателя у дефекта нет.**
2. **Тег `nova-duckdb`** — держит всё хранилище (8 карточек) и виды через `/api/history`.
   Цепь из пяти шагов записана в плане 01: вливание D471 → свежий оракул → **мой прогон
   пакета** → тег на три зеркала → строка зависимости здесь.
3. **T2.18** — ЗАКРЫТ ответами интегратора 2026-09-09: имя схемы ошибки в Polaris делается
   параметром; `x-data-class`/`x-example` — наш слой, не генератора; перечисление `4xx/5xx`
   ослабляется до `default`. **Записано в карточку, `8d6c0e5`.** Ждёт только параметра в Polaris — это работа интегратора, не этой карточки.

## 4. Находка по D471 — У НЕЁ ТЕПЕРЬ ЕСТЬ НОМЕР И ВЛАДЕЛЕЦ: №1061

**D471 (`cxx = true`) ломает сборку у первого настоящего носителя.** Влит 10:39
(`515daaafe`). Дописал ключ в `nova-duckdb/nova.toml` — `CC-FAIL`: `nova_rt.h` не
компилируется как C++ (`const char[33]` в поле `const uint8_t*`, строки 103, 359, 371).
Убрал ключ — `PASS: 1 FAIL: 0` (21 тест). Переменная одна. Манифест возвращён в
закоммиченное состояние, пакет зелёный. **Его негативная фикстура проверяла ГРОМКОСТЬ
отказа при отсутствии драйвера; положительный путь не проходил ни один реальный пакет.**

**РАЗРЕШИЛОСЬ НА CLANG 2026-09-09 20:36: обе стороны зелёные.**
`cxx = true` → `PASS: 1 FAIL: 0`; без `cxx` → `PASS: 1 FAIL: 0`. Интегратор прошёл три
редакции: заголовки (93 ошибки → 0, `82b7121af`), затем один `-x c` на всю команду
(сломалось на `libuv.lib`, его читало как исходник), затем ПАРА `-x c`/`-x none`
у каждого из 20 исходников — зелено.

**Четыре захода — четыре РАЗНЫХ состояния, и три из них похожи на движение:**
93 ошибки в заголовках (предмет НЕ достигнут) → одна ошибка в генерируемом C
(достигнут, отвергнут) → падение на архиве (СНОВА не достигнут, а выглядело
прогрессом) → зелено (достигнут и принят). Урок в памяти:
[[error-gone-may-mean-stopped-earlier]].

**Что ОСТАЛОСЬ открытым и почему это не формальность:** флаг читается ТОЛЬКО
в ветке clang. `Msvc` (1720) и `Gcc` (1901) его игнорируют, хотя значение до них
доезжает (протяжка на 1011) — то есть спросить есть где, а не спрашивают.
На двух третях цепочек остаётся молчаливое падение на линковке — четвёртый
пункт приёмки №1061.

**Прежняя запись 18:28:** интегратор завёл **№1061** (коммит `3f5e75e2f`)
с формулировкой точнее моей: «заголовки рантайма не компилируются как C++, то есть D471 —
флаг, который не работает НИГДЕ». Моё утверждение было о НОСИТЕЛЕ, его — о КЛАССЕ,
и класс здесь настоящий предмет. **Держать её в голове больше не нужно.**
Моё дело осталось одно: когда он скажет «починено» — повторить тот же A/B и дать два числа.

## 5. `nova-sdl` — второй дом, фаза Ф.3, восемь коммитов

`746cb88` каркас · `f989a09` вердикт спайка · `2a66989` близнецы сборки и их сторож ·
`d8345cf` сабмодули по тегам и проверка инструментов · `c6a5bc7` переопределение
объявлено в игноре · `7d00a9d` `tray.nv` и `text.nv` · `2a3b38c` сторож пары
FFI · `9ac6fb7` единая точка входа для сторожей.

**Все четыре модуля проходят чекер: `PASS: 4 FAIL: 0`**, без предупреждений. Написаны
ПОСЛЕ упора в инструменты, потому что `nova check` НЕ линкует — это главный
рычаг фазы: вся Nova-поверхность достижима без собранного SDL.

**Два сторожа и точка входа:** `python scripts/check-all.py` — находит сторожей
глобом, не списком. `check-build-twins.py` держит близнецов сборки,
`check-ffi-twins.py` — пару `ffi.nv` ↔ `sdl_shim.h` по именам в обе стороны.
**Известная дыра второго, названная в его шапке:** типы не сверяются, ширина
аргумента может разойтись незамеченной.

**Вопрос 2 спайка отвечен находкой: `#thread_affine` НЕ пересекает границу модуля.**
Пять проб и положительный контроль в `probes/thread-affine/`; через фасад тихо, напрямую
тихо, в `supervised` тихо, а та же форма с `extern` В ТОМ ЖЕ ФАЙЛЕ даёт
`E_THREAD_AFFINE_IN_FIBER`. В корпусе обе негативные фикстуры внутрифайловые — фикстуры
через модуль нет, оттого пробел и стоял. Вторая защита РАБОТАЕТ:
`E_LINEAR_CAPTURE_IN_FIBER` потребовал `spawn consume win`.

**Сабмодули на месте и пришпилены:** SDL `release-3.4.16`, SDL_ttf `release-3.2.2`.
Не `main`: пакет на движущейся ветке завтра собирается иначе без записанной причины.

**Два решения по сборке, которые нигде больше не записаны:**
- **ключ кэша хеширует САМИ ФЛАГИ**, а не сделанную раньше копию — иначе смена флага
  даёт «cache hit» и не собирает ничего (урок `nova-duckdb`, стоил часа);
- **таблица исключений сторожа близнецов держится ПУСТОЙ с причиной**: SDL3 на C,
  оба близнеца передают одно и то же, и непустая таблица означала бы расхождение,
  которое кто-то извинил вместо того чтобы починить.

**Не сделано:** сама сборка (инструментов нет, см. пункт 7), **тела десяти функций
шима** для трея и текста (объявлены в `sdl_shim.h`, помечены как нереализованные:
C, который нельзя скомпилировать, нельзя и проверить), вопросы 1 и 3 спайка.
**И одна названная течь:** `Window` экспортирован с `ro h`, то есть потребитель
дотянется до сырой ручки; `text.nv` этим пользуется. Чинится внутренним
аксессором вместе с телами модулей.

## 6. Чему научился за смену — это дороже остального

**Четыре моих собственных замера ответили на СОСЕДНИЙ вопрос вместо заданного.** Все
четыре нашлись перепроверкой, и каждый выглядел правдоподобным числом:

| мера | спрашивала | отвечала |
|---|---|---|
| выходы существуют | написана ли карточка | существует ли путь (четыре карточки делят один файл) |
| маркер в диапазоне карточки | несёт ли карточка отметку | есть ли слово между двумя карточками |
| счёт ключа в файле | реализовано ли | встречается ли слово, хоть в комментарии |
| словарь вердиктов | судима ли карточка | употребил ли я это слово и в этом регистре |

**Что работает вместо счёта — имя теста.** `parse.nv` обрабатывает `Retry-After`, и
доказательство — `test "Retry-After wins, and without it the floor is 60"`, хотя само
слово в файле встречается один раз и в докстроке.

**Урезание собственным конвейером.** `ls | head` скрыл файл, и я объявил его
отсутствующим. У соседнего окна `tail -4` съел строку живого дефекта. `head`/`tail`
законны для просмотра и запрещены, когда вывод — улика или основание вывода об отсутствии.

**Пустой контроль выглядит как успех.** Проба `#thread_affine` дала четыре чистых
результата подряд; они значили бы «всё хорошо» и «страж не подключён» одинаково, пока не
появился положительный контроль. Ломать пробу надо в том месте, которое чинишь.

**Компилятор — источник истины, не память.** За сегодня он поправил четыре мои формы:
многострочный `import` (нельзя), сосед по папке (`import pkg.ffi` + `ffi.имя()`), вызов
`extern` не требует `unsafe`, consume из `Ok(..)` требует `Ok(consume x)`.

## 7. Что делать первым при перезапуске

1. Прочитать этот файл и `git log --oneline -20` в обоих деревьях.
2. Хвостов без машины и без чужого слова НЕ осталось: T2.18 записан (`8d6c0e5`),
   снимок обновлён. Всё оставшееся ждёт слота либо чужих рук.
3. **Сборка SDL ЖДЁТ НЕ СЛОТА, А ИНСТРУМЕНТОВ — замер 2026-09-09 18:40.**
   `cmake` и `ninja` ОТСУТСТВУЮТ на этой машине: нет ни в bash (`command -v`), ни
   на PATH Windows (`Get-Command`), ни в обычных корнях, ни в дереве `nova`, ни в
   `vcpkg`. `clang` есть — `C:\Program Files\LLVM\bin\clang.exe`, и компилятор находит
   его по ЯВНОМУ списку путей (`find_clang_path`, `test_runner.rs:514`), поэтому
   `nova test` печатает `Toolchain: clang`, а оболочка его не видит.
   **Установка инструментов — решение владельца, не тихое действие окна.**
   Побочное следствие, которое стоит знать: `nova-duckdb` собран и его `.lib`
   лежат в `native/lib`, но **чистый клон здесь пересобрать их не сможет** — его скрипту
   нужен тот же cmake. Это касается шага 3 цепи до тега.
   После установки — одна команда: `cd nova-sdl && ./scripts/build-sdl.sh`, затем
   вопросы 1 и 3 спайка. Независимо: повторить A/B по D471 (№1061), когда
   интегратор починит `nova_rt.h`.
4. Открытый вопрос владельцу: показать ли ему страницу — `python scripts/preview.py`,
   http://127.0.0.1:7392/, данные из фикстур, живых обновлений нет намеренно.
