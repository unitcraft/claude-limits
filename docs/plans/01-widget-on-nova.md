# План 01 — claude-limits на Nova: бэкенд с браузерным UI + опциональный виджет поверх окон

**Статус:** ПРЕДЛОЖЕН 2026-09-05, **переработан 2026-09-06**, не начат. Стек назван
владельцем: **Nova**. Форма названа владельцем 2026-09-06: **бэкенд, который смотрят
через браузер, плюс небольшой виджет поверх окон — опционально**. До этого план
строил один SDL-виджет; его материал сохранён в §2 и Ф.3, но виджет теперь второй
фронтенд, а не основа. Вопросы §10 закрыты владельцем 2026-09-05 и 2026-09-06.
Запуск — по слову владельца.

Все факты о языке и пакетах ниже сверены по дереву `<repos>` 2026-09-05 и
2026-09-06 (путь к файлу указан у каждого). Факты о SDL3 — по wiki.libsdl.org и
исходникам `libsdl-org/SDL` (`main`) 2026-09-05.

## 0. Что строим

Один бинарь `claude-limits`, один процесс, три режима:

| режим | что делает | кому |
|---|---|---|
| `claude-limits` (по умолчанию) | **бэкенд**: находит учётки, опрашивает эндпоинт раз в `interval_sec` (300 с по умолчанию, пол 60 с), держит снимок, отдаёт HTTP на `127.0.0.1:7391`: страницу с барами, JSON, SSE | основной сценарий: вкладка браузера или закреплённое окно браузера |
| `claude-limits --widget` (или `[widget] enabled = true`) | то же **плюс** SDL3-окно без рамки поверх других окон и иконка в трее, в том же процессе | владельцу на рабочем столе |
| `claude-limits --once` | таблица в формате `claude_limits.py` и выход | отладка, дифференциал Ф.1 |

**Страница в браузере** (образец — скриншот claude-usage-widget): тёмная, шапка с
названием, временем последнего обновления и кнопкой «обновить». Ниже — **группа на
учётку**: строка-заголовок `почта (организация) [каталог]`, под ней три бара:

| бар | источник в ответе | подпись под баром |
|---|---|---|
| Current session (5 h) | `limits[kind=session]` | `reset 20:49 (1h 52min)` |
| All models (7d) | `limits[kind=weekly_all]` | `reset Tue 12:59 (2d 18h)` |
| `<Model>` only (7d) | `limits[kind=weekly_scoped]`, по одному на модель | то же |

Процент — по центру бара. Цвет — по `severity` (`normal` / `warning` / `critical`)
и по `locked_reason`. Учётка с протухшим токеном или ошибкой сети — серая группа с
текстом причины вместо баров, не пропуск. Страница обновляется сама по SSE, без
перезагрузки; при недоступном SSE — опрос JSON раз в 10 с. Без сборки фронтенда:
один `index.html` с ванильным JS, встроенный в бинарь.

**HTTP API** (все ответы JSON, кроме страницы и SSE; полный контракт — схемы, ошибки,
события SSE, доступ из LAN, OpenAPI — подплан [01.3-api.md](01.3-api.md)):

| путь | ответ |
|---|---|
| `GET /` | `index.html` |
| `GET /api/snapshot` | снимок: `fetched_at`, `accounts[] { email, org, dirs[], state: ok/stale/unknown/error, message, next_poll_at, limits[] { kind, model, percent, severity, resets_at, reset_label } }`, `worst { percent, severity }` |
| `GET /api/events` | SSE: событие `snapshot` с тем же телом после каждого опроса; `ping` раз в 30 с |
| `POST /api/refresh` | опросить сейчас; ответ — 202 |
| `GET /api/history?range=24h\|7d\|30d&by=account\|folder` | замеры раз в 5 мин за период: по учётке — `points[] { at, kind, model, percent, locked }`; по папке — то же плюс `occupancy[] { from, to, email }` (дизайн, §3.3) |
| `PUT /api/config` | настройки из интерфейса: папки, порядок учёток, вид, стиль бара, интервал, пороги, `[server]`; валидация как при старте, ошибка — 400 с текстом |
| `GET /api/health` | `ok`, версия, аптайм |
| `GET /api/config`, `POST /api/folders/probe`, `POST /api/config/token`, `GET /api/export`, `GET /api/openapi.json` | настройки, проверка папки до добавления, генерация токена LAN, выгрузка истории, описание API — подплан 01.3 |

Токены учёток в API не попадают никогда (README §Rules), только почта, организация,
имя каталога и проценты.

**Сеть**: слушаем только `127.0.0.1` по умолчанию. `[server] bind = "0.0.0.0"` — явное
решение пользователя для доступа с телефона в локальной сети, и тогда обязателен
`access_token` в конфиге: API — `Authorization: Bearer`, страница — `?token=` один раз,
дальше cookie. Без токена при `bind` не на loopback бэкенд отказывается стартовать.
TLS на локальном сервере не делаем (§9).

**Виджет** (опционально): окно без рамки, **поверх других окон**
(`SDL_WINDOW_ALWAYS_ON_TOP`, переключается из меню трея), позиция запоминается в конфиге;
внутри — та же раскладка групп и баров, что на странице; перетаскивание за шапку;
«закрыть» в шапке прячет окно. **Трей**: иконка с цветом худшего `severity` и цифрой
худшего процента; тултип — строка на учётку; меню — «Показать/Скрыть окно»,
«Открыть в браузере», «Обновить», «Поверх окон» (галочка), «Автозапуск» (галочка),
«Выход». Клика по самой иконке в API трея SDL3 нет — колбэки есть только у пунктов
меню (§2). Виджет читает снимок из памяти процесса, не через HTTP.

Данные и правила обращения с токенами — как в [README](../../README.md)
§How it gets the data, здесь не повторяются. **Правила опроса** — из эталона
`scripts/claude_limits.py` (2026-09-06), переносятся один в один:

- каталоги с одной почтой — **одна учётка** с перечнем каталогов в подписи, эндпоинт
  спрашивается один раз на учётку;
- с протухшим по `expiresAt` токеном запрос не делается: сервер ответит 401, а затем
  429 всей машине; учётка сразу помечается `stale`;
- HTTP 429 — состояние `unknown` с текстом «сервер просит подождать N с», следующий
  опрос не раньше `Retry-After` (или пола 60 с, если заголовка нет);
- `interval_sec` ниже 60 поднимается до 60 с предупреждением в лог.

## 1. Решения

| № | решение | почему |
|---|---|---|
| 1 | **Стек — Nova**, приложение = пакет Nova с `[[bin]]` | слово владельца; побочная выгода та же, что у плана 274 §0: настоящее приложение вскрывает дыры языка и std |
| 2 | **Основной UI — страница в браузере**, сервер — **`nova-polaris`** (`Router`, `serve_router`, `ServerResponse.sse`, `embed_dir` + `static_handler`) | слово владельца 2026-09-06; всё нужное уже в пакете: `examples/06-static-site` (встроенная статика), `examples/07-sse-stream` (SSE через `StreamBody`); ноль C-кода в основном сценарии |
| 3 | **Один бинарь, один процесс**: бэкенд всегда, виджет — флагом или конфигом, в том же процессе | виджет читает снимок из памяти без второго HTTP-клиента; одна точка автозапуска; отдельный процесс виджета — не нужен, пока не понадобится виджет на другой машине (§9) |
| 4 | **Виджет — SDL3 + SDL_ttf** через пакет `nova-sdl` | §2: единственная C-библиотека, дающая трей, окно без рамки и топмост на обеих ОС одним API; SDL_ttf — хинтинг на мелком кегле |
| 5 | **HTTPS-клиент — `nova-http` + `nova-tls`**, JSON — `std/encoding/json.nv` | уже есть, тянутся как git-зависимости по тегам (`nova-http` v0.1.3, `nova-polaris` v0.1.6 на 2026-09-06) |
| 6 | **Учётки — только каталоги Claude Code**, чтение без записи, **`refreshToken` не трогаем никогда** | README §Rules; решение владельца 2026-09-05 |
| 7 | **Loopback по умолчанию**, сеть — явно и только с токеном | страница показывает почты и организации; случайно открыть их в LAN нельзя |
| 8 | **Файберы**: UI SDL — только в `main`; accept-loop Polaris — в `spawn`; поллеры — `spawn` по учётке; всё под одним `supervised` | SDL требует главный поток (§3.1), Polaris — любой файбер с `Net Time Detach` |
| 9 | **Привязка к SDL — отдельный пакет `nova-sdl`**: сиблинг-репо, `.nv`-фасад + вендоренный C, как `nova-tls`; виджет тянет его по тегу | решение владельца 2026-09-05 |
| 9а | **Windows — один статический `.exe`**, без `SDL3.dll` | решение владельца 2026-09-05 |
| 9б | **Linux без StatusNotifierWatcher — виджет без трея**, второго бэкенда трея нет | решение владельца 2026-09-05; теперь это ещё менее болезненно: страница в браузере есть всегда |
| 10 | **Приёмка ядра — дифференциал с `scripts/claude_limits.py`**; приёмка API — дифференциал `curl /api/snapshot` с тем же `claude_limits.py` | эталон уже есть |
| 11 | **Без упрощений «как для прода»**: фаза не закрывается заглушкой | норма проектов владельца (dev-workflow §4) |
| 12 | **Хранение**: настройки — один TOML, данные — встраиваемая БД через спутниковый пакет (`nova-sqlite` или `nova-duckdb` — подплан [01.4](01.4-nova-duckdb.md), решение владельца по спайку в подплане 01.2 §0а–0б), одно соединение у файбера `store`; схема, пути, ретенция — подплан [01.2](01.2-storage.md) | настройки правит человек, данные — сотни тысяч замеров с запросами по времени; амальгамация SQLite собирается `vendor_src_dirs`, привязка — пример №1 ffi-cookbook |

## 2. UI: что рассматривалось

Требования до 2026-09-06: трей + окно на Windows и Linux одной C-библиотекой. После:
**смотреть через браузер** — основное; виджет поверх окон — опционально. Таблица
оставлена целиком: она объясняет, почему для виджета остаётся SDL3, а для основного
UI — браузер.

| вариант | трей | окно и рисование | поверх окон | цена | роль |
|---|---|---|---|---|---|
| **страница в браузере над `nova-polaris`** | нет | HTML/CSS, бары — три `div`, ноль C | нет (вкладка) | ноль нативного кода; доступ с телефона | **основной UI** |
| браузер `--app=http://127.0.0.1:7391` | нет | та же страница без интерфейса браузера | нет: окно чужого процесса | ноль | подсказка в README для тех, кому виджет не нужен |
| **SDL3** (+ SDL_ttf) | есть: `SDL_CreateTray`, меню, тултип — с 3.2.0; Windows = notification area; Linux = StatusNotifierItem по D-Bus (`src/tray/unix/SDL_dbustray.c` в `main`; в релизах 3.2.x — через GTK3 + libayatana-appindicator, грузятся динамически) | окно без рамки (`SDL_WINDOW_BORDERLESS`), перетаскивание через `SDL_SetWindowHitTest`, `SDL_Renderer` рисует прямоугольники; текст — SDL_ttf | `SDL_WINDOW_ALWAYS_ON_TOP`; Windows и X11 — да, Wayland — нет (см. ограничения) | две библиотеки одной семьи, одна сборка CMake, статический `.exe` | **виджет** |
| webview-библиотека (WebView2 / WebKitGTK) | нет | та же страница в своём окне | через нативный хендл | две реализации трея, webkit2gtk на Linux, WebView2 (COM/C++) на Windows, статический `.exe` невозможен | нет |
| GTK3 | Linux — appindicator; Windows — `GtkStatusIcon` снят | виджеты готовые | X11 only | ~30 МБ DLL на Windows | нет |
| Dear ImGui, Nuklear, Clay | нет | нужен бэкенд SDL/GLFW | от бэкенда | SDL всё равно нужен | нет: макет — стопка прямоугольников и текст |
| Tcl/Tk (стек образца claude-usage-widget) | нет; образец берёт панель задач из Win32 через `ctypes` | Canvas | `-topmost` | вторая реализация для Linux | нет |

**Почему SDL_ttf, а не `stb_truetype`** (для виджета). §5 всё равно собирает SDL3
CMake-скриптом (build-and-cache), SDL_ttf с вендоренным FreeType собирается тем же
скриптом. Взамен — хинтинг и кернинг на мелком кегле («reset Tue 12:59 (2d 18h)» на
100 % масштабе). HarfBuzz выключаем (`SDLTTF_HARFBUZZ=OFF`): шейпинг для латиницы и
кириллицы не нужен.

**Известные ограничения, которые план принимает:**

- **SDL — один поток.** `SDL_CreateTray`, `SDL_UpdateTrays` — «только главный поток».
  Решение — §3.1.
- **Linux без StatusNotifierWatcher** (GNOME без расширения AppIndicator):
  `SDL_CreateTray` возвращает NULL с `no SNI support`, фолбэка в SDL нет. Виджет живёт
  окном без трея с подсказкой в шапке; страница в браузере не зависит от этого вовсе.
- **Версия SDL.** D-Bus-бэкенд трея есть в `main`; какой релиз его несёт — выясняет
  Ф.3-спайк, возможен исход «закреплять коммит `main`». Ubuntu 24.04 (runner CI)
  пакета `libsdl3` не имеет — собираем из исходников.
- **Нет клика по иконке трея.** В `SDL_tray.h` колбэк только у пункта меню
  (`SDL_SetTrayEntryCallback`). Показ окна — первый пункт меню.
- **Wayland: нет «поверх всех окон».** SDL3 флаг игнорирует под KDE и GNOME Wayland.
  Обход — `SDL_HINT_VIDEO_DRIVER = "x11"`, окно идёт через XWayland, композиторы
  уважают `_NET_WM_STATE_ABOVE`; трей от этого не зависит. Если и так не держится —
  строка в шапке «поверх окон недоступно в этой сессии», и у пользователя остаётся
  браузер.
- **Полноэкранные приложения.** Windows скрывает топмост-окна под эксклюзивным
  полноэкранным приложением. Норма, у образца так же.
- **SSE через прокси и «спящие» вкладки.** Браузер сам переподключает `EventSource`;
  страница дополнительно опрашивает `/api/snapshot`, если событий нет дольше 90 с.

## 3. Архитектура

### 3.1. Файберы — главный инвариант

Рантайм Nova спавнит тело `main` файбером, **закреплённым за настоящим главным потоком
процесса** (`spec/decisions/06-concurrency.md`, комментарий у `nova_fiber_spawn_into`:
«pinned to THIS calling — the true process main — OS thread»). Отсюда:

```
main
└── supervised {
      spawn  store          — владелец Snapshot: принимает результаты поллеров,
                              раздаёт снимок по запросу, шлёт «обновилось» подписчикам SSE
      spawn  poller[i]      — по учётке: читает файлы, HttpClient -> эндпоинт, Duration sleep
      spawn  serve_router   — accept-loop Polaris; per-connection файберы detach'атся им самим
      (main) widget loop    — только при --widget: SDL_PollEvent + рисование + Time.sleep
      (main) иначе          — ждёт завершения supervised (Ctrl+C / «Выход» из меню)
    }
```

- все `extern "C"` SDL объявляются **`#thread_affine`** (D172: вызов через границу
  `spawn` — ошибка компиляции `E_THREAD_AFFINE_IN_FIBER`). Компилятор сам не даст
  позвать SDL из хендлера или поллера;
- цикл виджета в `main`: `SDL_PollEvent` неблокирующий → отрисовка при изменениях →
  `Time.sleep(16 ms)`; при скрытом окне — `Time.sleep(250 ms)`. **Не**
  `SDL_WaitEventTimeout` и **не** `#blocking`: `#blocking` уносит вызов на threadpool
  и ломает привязку к потоку;
- `serve_router(listener, app, policy)` требует `Net Time Detach` и сам владеет
  accept-loop'ом (`nova-polaris/docs/serving.md` §Running the accept loop) — поэтому
  он в `spawn`, а не в `main`;
- **общее состояние.** В std нет `Mutex`/`RwLock` (`std/src/concurrency/`: `nvchan`,
  `supervisor`, `cancellation`, `timer`, `rate_limiter`, `retry`). Polaris-примеры
  делят состояние захватом `mut`-локалов в замыканиях хендлеров
  (`examples/10-mini-service/src/main.nv:76-81`). Для нас: хендлеры захватывают
  канал к `store` и спрашивают снимок через request/reply `Chan`; SSE-хендлер
  подписывается на `Chan[Snapshot]` и превращает его в `stream_body` (образец —
  `examples/07-sse-stream`, pull-based продюсер). Могут ли detach'нутые файберы
  соединений бежать на другом OS-потоке параллельно с поллерами — вопрос Ф.0 п.4:
  если да, захват `mut` небезопасен и `store`-файбер обязателен; если нет — он всё
  равно остаётся ради SSE-фан-аута.

Ф.0 проверяет инвариант замером, а не рассуждением (§6).

### 3.2. Модули

Два репозитория. **`nova-sdl`** — пакет-привязка (решение 9), только он трогает C,
нужен только для Ф.3:

```
nova-sdl/                     сиблинг-репо, раскладка как у nova-tls
├── nova.toml                 [package] name="sdl" + [ffi] (§5)
├── sdl.nv                    фасад: init/quit, окно, рендер, события, топмост
├── tray.nv                   трей: иконка из растра, тултип, меню с галочками, колбэки
├── text.nv                   SDL_ttf: открыть шрифт, строка -> текстура, измерение
├── ffi.nv                    extern "C" #thread_affine — тонкий слой (ffi-cookbook «Layered FFI»)
├── native/
│   ├── sdl_shim.c / .h       плоские сигнатуры над SDL и SDL_ttf
│   ├── SDL/                  git-сабмодуль (релиз или коммит main, §2)
│   └── SDL_ttf/              git-сабмодуль, FreeType из external/, HarfBuzz выключен
└── scripts/build-sdl3.ps1 / .sh   сборка в native/lib (build-and-cache, прецедент libuv)
```

**`claude-limits`** — приложение, чистая Nova, без своего C:

```
claude-limits/
├── nova.toml                 [package] + [[bin]] name="claude-limits" path="bin/claude_limits.nv"
│                             + [dependencies] http, tls, polaris, sqlite, sdl (git, по тегам)
├── bin/claude_limits.nv      main: аргументы → конфиг → supervised { store; поллеры; сервер; [виджет] }
├── accounts/                 обнаружение каталогов Claude Code, чтение двух файлов
│   ├── discover.nv           default dir, CLAUDE_CONFIG_DIR, список, родитель, UNC-пути WSL (Fs, Os)
│   ├── login.nv              Login { email, org, dir, token, expires_at }            (Fs)
│   └── group.nv              []Login -> []Account: одна почта = одна учётка, dirs[]  — чистое
├── usage/
│   ├── fetch.nv              fetch(token) -> Result[Usage, UsageError]; 429 -> RetryAfter(sec) (Http, Time)
│   └── parse.nv              JsonValue -> Usage { limits []Limit, locked, fetched_at }
├── model/
│   ├── snapshot.nv           Snapshot, AccountState, worst(); чистое
│   ├── store.nv              store-файбер: Chan[PollResult] -> Snapshot; request/reply; подписки SSE
│   └── format.nv             «reset Tue 12:59 (2d 18h)», подписи — чистое, общее для страницы и виджета
├── storage/                  подплан 01.2
│   ├── paths.nv              каталоги конфига/данных/логов по ОС, portable-режим                   (Os, Fs)
│   ├── config_file.nv        чтение/валидация/канонический TOML, write_atomic, If-Match         (Fs)
│   ├── db.nv                 открытие, прагмы, миграции из embed_dir("migrations"), бэкап, quick_check
│   ├── repo.nv               запись тика одной транзакцией; выборки для /api/history, прогноза, плиток
│   └── maintenance.nv        дневные свёртки, ретенция, incremental_vacuum
├── migrations/0001_init.sql  схема из подплана 01.2 §3
├── server/
│   ├── routes.nv             Router: /, /api/snapshot, /api/events, /api/refresh, /api/health
│   ├── auth.nv               Bearer / cookie по access_token; отказ старта при bind≠loopback без токена
│   └── json.nv               Snapshot -> JSON (без токенов — тест §7)
├── web/
│   └── index.html            одна страница: разметка, CSS (тёмная тема), JS (EventSource + fetch)
├── widget/                   только при --widget; всё через пакет sdl
│   ├── layout.nv             чистые функции: прямоугольники групп и баров из Snapshot и размеров окна
│   ├── draw.nv               Snapshot + Layout -> вызовы рендера
│   └── tray.nv               растр иконки по severity, тултип, пункты меню
├── config.nv                 claude-limits.toml (std toml.nv)
├── scripts/
│   ├── claude_limits.py      эталон пути данных и правил опроса (есть; --daemon, 429, группировка)
│   ├── start-daemon.ps1      запуск эталона демоном на Windows (есть)
│   ├── diff-with-probe.sh    дифференциал Ф.1 (--once) и Ф.2 (curl /api/snapshot) с claude_limits.py
│   └── smoke-server.sh       поднять, дёрнуть /api/health, /api/snapshot, 3 события SSE, погасить
└── docs/plans/               этот план
```

Эффекты в сигнатурах — по канону module-conventions: `accounts/` зависят от `Fs`/`Os`,
`usage/` — от `Http`/`Time`, `server/` — `Net Time Detach` через Polaris, `model/` без
`store.nv`, `format.nv` и `widget/layout.nv` — чистые. Это и есть тестируемость без
моков: в тестах подменяются хендлеры (§7).

### 3.3. Страница `web/index.html`

- Встраивается через `embed_dir("web")` (прелюдия, `std/src/prelude/embed.nv`) и
  отдаётся `static_handler` (`nova-polaris/docs/static-files.md`); ETag и mime — от
  Polaris.
- Вёрстка повторяет виджет-образец: один столбец, группы по учёткам, бар — `div` с
  `border-radius`, процент по центру, подпись под баром. Цвета severity — CSS-переменные;
  тёмная тема по умолчанию, светлая — по `prefers-color-scheme`.
- JS: `new EventSource('/api/events')`, на `snapshot` — перерисовать; таймер 90 с без
  событий — `fetch('/api/snapshot')`; кнопка «обновить» — `POST /api/refresh`.
  Форматирование «reset … (2d 17h)» приходит готовым из `format.nv`, чтобы страница и
  виджет показывали одну и ту же строку и разница ловилась дифференциалом.
- Ни фреймворков, ни сборки, ни внешних ресурсов: страница работает с `file://`-строгой
  CSP; шрифты Manrope и JetBrains Mono встраиваются в бинарь вместе со страницей (как на
  сайте Nova), без обращения к Google Fonts.

**Дизайн утверждён владельцем 2026-09-06** — [docs/design/browser-page](../design/browser-page/README.md):
артборды `*.dc.html` там же служат эталоном разметки для `web/index.html`. **Поэлементная
спецификация** — что каждый элемент значит, откуда берёт данные, как ведёт себя, формулы
доли времени и прогноза, контракты `/api/history` и `PUT /api/config`, ключи конфига,
приёмка Ф.2 — в подплане [01.1-browser-page-spec.md](01.1-browser-page-spec.md). Что дизайн добавляет
к объёму сверх написанного выше:

- **три вида** в шапке — список (по умолчанию), плашки с перетаскиванием (порядок учёток
  пишется в конфиг), статистика;
- **два стиля бара** — закруглённый и кубики (ячейка 8 px + зазор 2 px, заливка и ширина
  округляются до целой ячейки; `repeating-linear-gradient`, не N элементов);
- **статистика**: три графика по трём окнам лимитов; группировка по учёткам и по папкам
  (папка = человек, в ней сменяются учётки; линия берёт цвет учётки и обрывается на смене).
  Для этого бэкенд ведёт **историю замеров** (SQLite, таблица `sample`, 5 мин, 30 дней) и
  **журнал занятости папок** (таблица `occupancy`: какая почта сидела в каком каталоге когда;
  схема — подплан 01.2 §3), отдаёт
  `GET /api/history?range=24h|7d|30d&by=account|folder`. История переезжает из Ф.5 в Ф.2;
- **настройки в интерфейсе**, включая добавление и удаление папок с кредами: страница пишет в
  конфиг через `PUT /api/config` (валидация та же, что при старте: `bind` не на loopback без
  токена — 400). Правило «пишем только `[widget]`» (§4) заменяется на «пишем только свой
  конфиг, никогда — каталоги Claude Code»;
- тема в токенах сайта Nova (`www/site/src/styles/global.css`), акцент на кнопках не
  используется, подкрашены только поверхности.

### 3.4. Данные во времени

- `resets_at` вида `2026-09-05T16:09:59.804639+00:00` разбирается
  `std/time/civil/parse.nv` (дробные секунды и смещение поддержаны — строки 100–145);
- локальное время — `TimeZone @offset_at` (`civil/zoned.nv`); часовой пояс машины —
  из `tz.nv`/`tzif.nv` (Linux) и Windows-путь проверяется в Ф.0;
- «(2d 18h)» — `Duration @to_human()` (`time/duration/core.nv:638`), если формат
  подходит; иначе свой форматтер из двух старших единиц — решает Ф.0 по выводу.
  Локальное время — время **сервера**; для браузера с телефона в другом поясе это
  принятое ограничение (JSON несёт и сырой `resets_at`).

## 4. Конфиг

Полный список ключей со значениями по умолчанию, расположение файла на каждой ОС,
portable-режим, правила чтения, атомарной записи и конфликтов — подплан
[01.2-storage.md](01.2-storage.md) §1–2; ниже — только суть.

`claude-limits.toml` рядом с бинарём или в каталоге конфигов пользователя
(`%APPDATA%\claude-limits\` / `~/.config/claude-limits/`):

```toml
interval_sec = 300                                 # пол 60 с: сервер отвечает 429 на частый опрос
accounts_parent = "C:/accounts"   # каждый подкаталог — учётка
accounts = ["~/.claude"]                           # плюс явные каталоги; UNC-пути WSL в одинарных кавычках

[thresholds]
warning = 70
critical = 90

[server]
bind = "127.0.0.1"                                 # "0.0.0.0" — только вместе с access_token
port = 7391
# access_token = "..."                             # обязателен при bind не на loopback

[widget]
enabled = false                                    # --widget включает без правки файла
always_on_top = true                               # переключается из меню трея, пишется сюда
x = 1480                                           # позиция; отсутствует — правый нижний угол
y = 820                                            # основного экрана над панелью задач
visible = true                                     # показывать окно при старте
```

Порядок поиска: `--config PATH`, затем `claude-limits.toml` рядом с бинарём или в корне
репозитория (гитигнорится: он называет учётки; образец — `claude-limits.example.toml`,
как у эталона), затем каталог конфигов пользователя. Нет файла — default-каталог и
`CLAUDE_CONFIG_DIR`, как у `claude_limits.py`, loopback, без виджета. Секция `[widget]` — единственное, что программа пишет сама (позиция, галочки);
каталоги Claude Code она не трогает никогда (решение 6). Позиция за пределами текущих
экранов — сброс в угол по умолчанию.

## 5. Нативная часть: сборка и `[ffi]` (всё — в пакете `nova-sdl`, только для виджета)

Канон — `[ffi]` в `nova.toml` (ffi-cookbook «Build pipeline»): `.c`-шим компилирует
clang, библиотека **линкуется уже собранной**. SDL3 и SDL_ttf собираются CMake, поэтому
`vendor_src_dirs` (как у mbedTLS в `nova-tls/nova.toml`) не подходит; берём прецедент
libuv «build-and-cache»: скрипт собирает сабмодули один раз в `native/lib`, повторный
запуск — no-op по хешу коммитов.

```toml
[ffi]
c_shims      = ["native/sdl_shim.c", "native/sdl_shim.h"]
include_dirs = ["native/", "native/SDL/include", "native/SDL_ttf/include"]
libs         = ["SDL3_ttf-static", "freetype", "SDL3-static"]
lib_dirs     = ["native/lib"]
```

Порядок `libs` — от зависимого к зависимости. SDL_ttf — с `SDLTTF_VENDORED=ON` и
`SDLTTF_HARFBUZZ=OFF`; точные имена `.lib`/`.a` фиксирует спайк по выводу сборки.

Windows: только статические библиотеки, один `.exe` без DLL (решение 9а); конфликт CRT
с рантаймом Nova, если проявится, — дефект в реестр и починка. Linux: `.a` + системный
libdbus (грузится динамически самим SDL; без него — виджет без трея). Пакет тегается
интегратором (AGENTS.md «Satellite package repositories»).

**Следствие решения 3**: `sdl` — зависимость бинаря всегда, `SDL_Init` вызывается
только при включённом виджете. На безголовом Linux без дисплея бэкенд работает,
`--widget` там честно падает с сообщением SDL. Если статическая линковка SDL в бинарь
без виджета окажется неприемлемой по размеру (замер в Ф.3) — feature-флаг пакета или
второй `[[bin]]`, решает владелец по цифре.

**Запрет** (из read-project §3): ничего из `compiler-codegen/` в этот репозиторий не
копировать — рантайм и std берутся из установленного тулчейна.

## 6. Фазы

Каждая фаза — со своей приёмкой; приёмка — строка прогона, не «сделано». Порядок
изменён 2026-09-06: сервер и страница раньше виджета.

### Ф.0 — разведка (спайки, не продукт)

Спайки в `spikes/`, каждый — программа на Nova и строка вердикта в `spikes/VERDICT.md`:

1. **Polaris из чистого пакета.** `Router` с `/`, `/api/snapshot` (фиктивный JSON),
   `/api/events` (SSE с тикером из `Chan`, не по таймеру), `embed_dir("web")` +
   `static_handler`; `serve_router` в `spawn` под `supervised`, `main` ждёт. Вердикт:
   `curl` получает страницу и JSON, `curl -N /api/events` получает 3 события,
   присланные в канал из другого файбера; на Windows и Linux; размер бинаря.
2. **HTTPS.** `HttpClient` с двумя заголовками к `api.anthropic.com/api/oauth/usage`,
   токен — из файла. Вердикт: HTTP 200 и тот же JSON, что у `claude_limits.py`; откуда
   `nova-tls` берёт корневые сертификаты на Windows и на Linux.
3. **Время.** Разбор `resets_at`, локальный пояс, `to_human`. Вердикт: строка
   `reset Tue 12:59 (2d 18h)` для трёх заданных моментов на обеих ОС.
4. **Общее состояние.** Поллер-файбер пишет счётчик в `store`, 20 параллельных
   `curl` читают `/api/snapshot`. Вердикт: значения монотонны, гонок нет; бегут ли
   файберы соединений на других OS-потоках (по `spec/decisions/06-concurrency.md` и
   замером) — записать; отсюда — обязателен ли `store` (§3.1).
5. **Хранилище из Nova.** Два кандидата, оба через спутниковый пакет: `nova-sqlite`
   (амальгамация через `vendor_src_dirs`) и `nova-duckdb` (**полная CMake-сборка репозитория с
   расширениями `core_functions` и `icu`, статически**, подплан 01.2 §0б — амальгамация DuckDB
   без расширений отвергнута спайком 2026-09-06, §0а). Вердикт: время сборки с кэшем и без,
   размер бинаря Nova, 10 000 строк в транзакции (для DuckDB — через Appender), выборка по
   диапазону через `sql\`…\``-параметры, отсутствие сетевых обращений и `~/.duckdb`.

Спайк, который не сходится, — не повод «упростить», а вход в реестр дефектов nova
(`docs/plans/221.1-bug-sweep.md`, `№TBD`), если дыра в языке или std.

### Ф.1 — ядро без UI

`accounts/` (с группировкой по почте), `usage/` (с 429), `model/snapshot.nv`,
`model/format.nv`, `config.nv` и режим `--once`, печатающий таблицу в формате
`claude_limits.py` без `--daemon`.

Приёмка: `scripts/diff-with-probe.sh` — обе программы на одних каталогах и одном
конфиге, группы и столбцы `kind / percent / severity / resets_at` совпадают побайтно
(время — в одном поясе); фикстура 429 — одинаковая строка состояния у обеих.
Тесты — §7, обязательны оба направления.

### Ф.2 — бэкенд и страница

`model/store.nv`, `server/`, `web/index.html`, поллеры с интервалом, `POST /api/refresh`,
`[server]` конфига с отказом старта при `bind` не на loopback без токена.

Приёмка:
- `scripts/diff-with-probe.sh --api`: `curl /api/snapshot`, приведённый к таблице,
  побайтно совпадает с `claude_limits.py`;
- `scripts/smoke-server.sh`: health, snapshot, три события SSE после `POST /api/refresh`,
  корректное завершение по сигналу;
- страница в браузере: группы по учёткам, обновление без перезагрузки — скриншот и
  строка в отчёте; отключить сеть — группы серые с причиной, не пусто;
- **негатив**: `bind = "0.0.0.0"` без токена — процесс не стартует с понятной строкой;
  с токеном — запрос без `Authorization` даёт 401, с — 200; в теле `/api/snapshot`
  нет подстроки `accessToken` и нет ни одного токена из фикстур (тест §7);
- с телефона в LAN страница открывается по `?token=` и дальше по cookie.

### Ф.3 — виджет (опционально для пользователя, обязательно для плана)

Спайк перед фазой — **рождение `nova-sdl`**: репозиторий-сиблинг по раскладке
`nova-tls`; сборка SDL3 и SDL_ttf скриптом на Windows и Linux; шим; `extern "C"` для
`SDL_Init`/`SDL_CreateTray`/`SDL_CreateWindow`/`SDL_PollEvent`/`TTF_Init`/
`TTF_RenderText_Blended`; иконка в трее с меню из одного пункта, окно без рамки с
флагом топмост и одной строкой текста. Вердикт: релиз (или коммит `main`) SDL,
закреплённый сабмодулем; статический `.exe` собрался и его размер; трей: где появился,
где `no SNI support`; **матрица топмоста**: Windows 11, KDE X11, KDE Wayland, GNOME
Wayland — «держится / нет / с `x11`-драйвером»; в `SDL_tray.h` нет колбэка клика по
иконке (или есть — §0 дополняется); текст на 100 % и 150 % читаем — скриншот.
**Потоки**: `main` крутит `SDL_PollEvent` + `Time.sleep(16 ms)`, поллер и
`serve_router` в `spawn`; кадры не проседают; **обратная проба**: вызов трея из
хендлера обязан дать `E_THREAD_AFFINE_IN_FIBER`. Выход — тег `nova-sdl` v0.1.0.

Фаза: `widget/`, `--widget`, `[widget]` конфига, трей с меню из §0, окно с той же
раскладкой, что страница (общий `format.nv`), топмост, перетаскивание с записью
позиции, HiDPI через `SDL_GetWindowDisplayScale`. Скруглённые бары — одна
текстура-капсула, растянутая по ширине; иконки шапки — отрезки через
`SDL_RenderGeometry`.

Приёмка: `layout.nv` — чистые функции, тесты на прямоугольники для 1/2/3 учёток и
0/1/2 scoped-моделей; сравнение со скриншотом владельца — глазами; окно остаётся над
развёрнутым браузером на Windows 11 и KDE X11; после перезапуска — на той же позиции;
«Открыть в браузере» открывает `http://127.0.0.1:7391`; CPU в покое < 1 %, при скрытом
окне — ниже; матрица трея: Windows 11, KDE, GNOME с расширением, GNOME без (окно без
трея, страница работает). Размер бинаря с SDL и без — цифра владельцу (§5).

### Ф.4 — упаковка

`nova build` → один бинарь на ОС; автозапуск бэкенда (реестр `Run` / `.desktop` в
`~/.config/autostart`) флагом `--install-autostart` и пунктом меню виджета; README —
установка, конфиг, как закрепить окно браузера `--app`; GitHub Actions:
`windows-latest` + `ubuntu-latest`, `nova test`, `smoke-server.sh` против записанного
JSON, сборка SDL по скрипту с кэшем. Тег `nova-sdl` v0.1.x с тем API, на который встал
виджет.

### Ф.5 — после релиза (не в объёме плана)

Уведомления по порогам (в браузере — Notification API, в виджете — трей); история в
кольцевом буфере бэкенда и график на странице; виджет как отдельный процесс, читающий
`/api/events` с другой машины.

## 7. Тесты

- **Где**: рядом с модулем, `<module>_test.nv` (канон Nova: `authoring-a-module` §5).
- **Чем**: подмена хендлеров эффектов — `Http` через `nova-http/src/client/mock.nv`,
  `Fs`/`Os` через `std/src/os/mock.nv` и аналог для `Fs`; сервер — `serve_once`
  Polaris без сокета (`nova-polaris/docs/overview.md`). Никаких сетевых тестов в
  `nova test`; живая сеть — `--once`, дифференциал и `smoke-server.sh`.
- **Фикстуры**: записанные ответы эндпоинта с вычищенными полями (`fixtures/usage-*.json`):
  обычный; `locked_reason` заполнен; `limits` пуст (фолбэк на `five_hour`/`seven_day`);
  `weekly_scoped` для двух моделей; невалидный JSON; HTTP 401; HTTP 500.
- **Каталоги**: обе раскладки (default с `.claude.json` РЯДОМ; `CLAUDE_CONFIG_DIR` с
  файлами ВНУТРИ), каталог без логина, токен с истёкшим `expiresAt` (запроса нет,
  состояние `stale` — мок `Http` считает вызовы: ноль), два каталога с одной почтой
  (одна учётка, оба каталога в `dirs[]`, эндпоинт спрошен один раз — правило эталона
  `claude_limits.py`), UNC-путь WSL в `accounts`.
- **429**: фикстура с `Retry-After: 120` — состояние `unknown`, `next_poll_at` через
  120 с; без заголовка — через 60 с; `interval_sec = 10` в конфиге — поднят до 60 с
  предупреждением.
- **Сервер через `serve_once`**: каждый маршрут; 401 без токена при включённом
  токене; JSON снимка не содержит ни одного токена из фикстур (поиск подстроки по телу);
  SSE-кадр — `event: snapshot` + `data:` с валидным JSON; конфиг `bind=0.0.0.0` без
  токена — ошибка конструктора сервера, не паника.
- **Оба направления** на каждый пункт: «делает» и «не делает» (не пишет в каталоги
  Claude Code — проверка мок-хендлером `Fs`, что записи не было; пишет только в
  `[widget]` своего конфига).
- **Обратная проба** на каждую фазу — сломать условие и увидеть красный (Ф.1
  дифференциал с подменённым `percent`; Ф.2 подменить токен в фикстуре и увидеть, что
  тест «нет токена в JSON» краснеет при нарочной утечке; Ф.3 `E_THREAD_AFFINE_IN_FIBER`).

## 8. Риски

| риск | мера |
|---|---|
| Файберы соединений Polaris бегут параллельно поллерам на других OS-потоках | Ф.0 п.4 замером; `store`-файбер владеет состоянием в любом случае |
| Утечка почт/организаций в LAN | loopback по умолчанию; отказ старта без токена (решение 7); негатив-тест |
| SSE через `StreamBody` не отдаёт кадры инкрементально | `examples/07-sse-stream` и `servernet/rt/streaming_smoke.nv` утверждают обратное; Ф.0 п.1 проверяет `curl -N`; провал — дефект в `nova-polaris`, не свой сервер |
| `embed_dir` не поднимает `web/index.html` из пакета с `[[bin]]` | Ф.0 п.1; образец `examples/06-static-site` |
| `nova-tls` без корневых сертификатов ОС на одной из платформ | Ф.0 п.2; при отсутствии — вход в реестр nova |
| Эндпоинт не документирован и может измениться | разбор терпим к лишним полям; при пустом `limits` — фолбэк; в UI — «формат ответа изменился» |
| Сервер отвечает 429 на частый опрос с одной машины (факт эталона) | пол 60 с, `Retry-After`, один запрос на почту, ни одного запроса с протухшим токеном; `POST /api/refresh` тоже подчиняется полу — ответ 429 клиенту с `next_poll_at` |
| Токен протух и нет сессии Claude Code | серое состояние «запустите Claude Code под этой учёткой»; refresh не делаем (решение 6) |
| Топмост не держится под Wayland | обход через X11-драйвер; иначе строка в шапке; браузер есть всегда |
| Клик по иконке трея не приходит из SDL3 | пункт меню (§0) |
| SDL_ttf + FreeType статически на Windows: конфликт CRT | спайк Ф.3 собирает всё вместе первым делом; провал — дефект в реестр, не откат на `stb_truetype` без слова владельца |
| Бинарь с SDL тяжёл для тех, кому нужен только бэкенд | замер в Ф.3; feature-флаг или второй `[[bin]]` — решение владельца по цифре (§5) |
| Привязка к потоку: SDL из файбера `main` ведёт себя не так, как обещает спека | спайк Ф.3 замером; при провале — вопрос владельцу с обеими цитатами |
| Дыры языка и std по дороге (первое серверное + первое GUI-приложение на Nova) | реестр `221.1` с `№TBD`, без обходов в этом репо; это заявленная выгода |

## 9. Не делаем

macOS (токены в Keychain, трей другой); ключ сессии браузера; свой refresh токена —
никогда; TLS на локальном сервере (loopback, а в LAN — токен); многопользовательский
доступ и учётки веб-интерфейса сверх одного токена; второй бэкенд трея на Linux;
`SDL3.dll` рядом с `.exe`; виджет отдельным процессом (Ф.5); фронтенд-фреймворк и
сборка JS; графики и история (Ф.5); автообновление; тему/скины сверх тёмной и светлой.

## 10. Вопросы владельцу

Закрыты 2026-09-05:

| вопрос | ответ | где в плане |
|---|---|---|
| привязка к SDL внутри репо или пакетом | **пакет `nova-sdl`** с первого дня | решение 9, §3.2, §5, Ф.3 |
| GNOME без расширения: окно без трея или второй бэкенд | **окно без трея**, второго бэкенда нет | решение 9б, §2 |
| трогать ли `refreshToken` вне Claude Code | **нет** | решение 6, §8 |
| Windows: статический `.exe` или `.exe` + DLL | **один статический `.exe`** | решение 9а, §5 |

Закрыты 2026-09-06:

| вопрос | ответ | где в плане |
|---|---|---|
| на чём делать UI | **бэкенд + просмотр через браузер; виджет поверх окон — опционально** | §0, решения 2–4, §2 |
| окно виджета поверх других окон | **да, по умолчанию**, переключатель в меню трея; Wayland — обход через X11-драйвер | §0, §2, §4 |
| показ окна по клику на иконку трея | **пунктом меню**: у SDL3 нет колбэка на клик по иконке | §0, §2 |
| текст в виджете: `stb_truetype` или SDL_ttf | **SDL_ttf** | решение 4, §2, §5 |

Открыты (не блокируют старт Ф.0–Ф.2, решаются к Ф.3 по цифрам):

| вопрос | варианты | когда |
|---|---|---|
| один бинарь с SDL внутри или второй `[[bin]]` без него | по размеру бинаря из Ф.3 | Ф.3 |
| порт по умолчанию 7391 | любой свободный выше 1024; 7391 — предложение | Ф.2 |
