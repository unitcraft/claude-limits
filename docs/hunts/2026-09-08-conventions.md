# Охота на МОИ РЕШЕНИЯ в странице и инструментах против КОНВЕНЦИЙ — 2026-09-08

**Кто:** охотник на opus, форма `oracle`. **Клетка:** `src/web/` и `scripts/` против
`docs/conventions/api.md` и `database.md`. `src/server/` и `src/model/` намеренно
исключены — по ним шла соседняя охота. **Приёмку делаю я, окно `nova-55`.**

Девять находок, у каждой каталог с `FINDING.md`, `cmd.sh` и работающей пробой; все
девять прогоняются. В `src/`, `docs/`, `scripts/`, `fixtures/` охотник не записал
ничего.

## Колонка «проверено мной»

**ДА** — открыл оба места сам. **нет** — не проверял, основание ни для чего.

| № | класс | дверь | проверено |
|---|---|---|---|
| Н1 | **сколько ждать перед повтором — решается в каждом месте заново**, четырьмя разными синтаксисами | **ДВЕРИ НЕТ, четыре места:** `format.js:162` (только число, потолка нет), `format.js:183` (обе формы RFC 9110, потолок 3600), `app.js:203` (`Number(after)` — форма HTTP-date теряется), `live.js:24` (константа, `Retry-After` не спрашивает). **Замер:** на один и тот же заголовок-дату путь `GET` ждёт **0,5 с**, кнопка — **300 с** | **ДА** |
| Н2 | **как печатается момент — решается в 14 местах**, тремя синтаксисами; `hour12` есть в пяти и нет в трёх | **ДВЕРИ НЕТ.** Поле `tz` из снимка присваивается в `app.js:295` и **не читается нигде**. `render.js:115` печатает подпись сброса в поясе БРАУЗЕРА, вытесняя серверный `reset_label`, ради которого записано отклонение D2 («два интерфейса должны показывать одну строку») | нет |
| Н3 | **панель настроек МОЛЧА выбрасывает значение**, и «что выбросить» решается по ПОСЛЕДНЕМУ СЕГМЕНТУ пути | **ЭТО МОЯ СЕГОДНЯШНЯЯ РЕГРЕССИЯ.** Тумблер `ui.countdown` переключается → тело `PUT` пустое, Save закрывает панель, и `localStorage.countdown` **не пишет никто**. Фильтр по листу съест первый же `widget.view`. Норма — `api.md:26` «Молчаливых исходов нет» | нет |
| Н4 | **чёрный список ПДн-имён у линтера рукописный** и уже короче нормы: 4 имени против 9 | `lint-openapi.py:43`. **Конвенция запрещает это прямым текстом** (`api.md:593-596`: «Ни один из этих списков не ведётся руками отдельно»), а ТОТ ЖЕ файл в шапке объясняет, почему список МАРШРУТОВ он копией не держит. Одно правило, один файл, два противоположных решения | нет |
| Н5 | **правило о тестовых данных держится КАТАЛОГОМ**, а тестовые данные живут ещё литералами в наборах и В ИМЕНИ ФАЙЛА | `check-fixtures.py` спрашивает «где», а не «что». Носители: `a@x.com` в девяти строках `test-reorder.mjs` — живой чужой домен, не RFC 2606; фикстура с почтой в ИМЕНИ проходит, и страж печатает эту почту в свой отчёт | нет |
| Н6 | **`check-plan-trees.py` не может покраснеть**: `sys.exit` в файле НЕТ ни одного, при собственных ветках отказа | и `lint-openapi.py` сверяет маршруты в одну сторону — маршрут, которого нет ни в одной таблице плана, проходит | нет |
| Н7 | **что показать при отказе — решает каждый обработчик**; пять вызовов, пять исходов, один молчит | `refresh` (`app.js:550-568`) — `try/finally` без `catch`: отвергнутый `fetch` не даёт человеку ни строки, и вызывающий промис не ждёт. Четыре из пяти написаны с `catch` — автор думал об этом, и пятый выглядит починенным | нет |
| Н8 | **форма идентификатора решается пофайлово**: три фикстуры UUID v7, четвёртая — `acc-0001` | ни один из пяти стражей не содержит слова UUID применительно к данным; на этой фикстуре стоят 27 тестов, формы не проверяет ни один | нет |
| Н9 | **границы приведения «текст с провода → число» у страницы нет** | `chart.js:31` приводит, `render.js:83` сравнивает сырьём. **Оговорка охотника, прочесть первой:** сегодня контракт отдаёт `percent` целым, носитель УСЛОВНЫЙ — оживает, когда доля станет дробной, как и требует `api.md:512` | нет |


## Н1 — ПРИНЯТА И ЗАКРЫТА 2026-09-09

**Проверил сам, замером, а не чтением.** `probes/hunt-conventions-retry-after/measure.mjs`
на одном заголовке-дате: путь `GET` — **500 мс**, кнопка — **300 000 мс**. Расхождение
в 600 раз, и короткая сторона — это не «подождать поменьше», а сервер, которого не
услышали: `Number(<дата>)` даёт `NaN`, `NaN` приходит в `retryDelay` как «заголовка
нет», и тот откатывается по экспоненте, будто ничего не сказано.

**Дверь:** `retryAfterSeconds` — единственное место, где заголовок читается. Обе формы
RFC 9110 10.2.3. Политику каждый вызывающий оставляет свою (кнопка при нечитаемом ждёт
минуту, `GET` откатывается по экспоненте) — это разные и правильно разные решения, но
заголовок они читают ОДИН.

**Что чуть не сломалось по дороге, и что это стоит запомнить.** Сводя четыре места в
одно, я свернул «нечитаемо» и «ноль» в один ответ. `test-format.mjs` покраснел: явный
`Retry-After: 0` — это «сервер готов», а не «сервер промолчал». Дальше он покраснел
второй раз и на более тонком: дата, УЖЕ ПРОШЕДШАЯ, — тоже не ноль, потому что 429
никуда не делся, и обязана давать умолчание. Разбор теперь возвращает честное
расстояние до срока, отрицательное для прошедшего, и каждый вызывающий решает сам.

Три разных ответа там, где я видел два, — и нашли это не мои рассуждения, а тесты,
написанные раньше по спеке. Ради таких случаев их и пишут раньше.

**Проба остаётся исполняемой** и теперь утверждает СОГЛАСИЕ путей плюс само число
300 000: согласие без числа прошло бы и на двух одинаково неверных ответах.

Полный набор после починки: **203 теста, 8 проверок, чисто** (было 202 — ни один не
потерялся).


## Противоречия — выбирать не охотнику

**П1.** `api.md:660` требует разброс ±20 % на повторах; `01.3:590` фиксирует голые 30 с
и 10 с. Код следует плану, а шапка того же файла ссылается на §17 именно за причиной
разброса. В таблице отклонений записи нет.

**П2.** Чёрный список имён в URL: конвенция называет девять (`api.md:140`), приёмка
плана — три (`01.3:675`), и сужение подписано «чёрный список конвенции», хотя им не
является.

**П3.** В каком поясе печатается подпись момента: `01.1:143` говорит «локальное
время», `01.3:38` и план 01 — «подписи в поясе бэкенда, оси в поясе браузера». Строка
сброса — подпись, а не ось.

## Что охотник обошёл, и это тоже результат

- `src/server/` и `src/model/` — по запрету, чтобы не дублировать соседнюю охоту.
- Дыры `check-page.py` (IP, inline) и «страж судит не то дерево» — **уже найдены
  предыдущим охотником, не переоткрывает**. Добавляет носителей того же класса:
  `check-dir-fixtures.py:10`, `check-config-fixture.py:9`, `check-plan-trees.py:3`.
- **Зазор назван, носителя нет:** `check-citations.py` проверяет ссылки только на
  планы; ссылки на конвенции (`convention section 17`) в коде есть — четыре штуки — и
  не проверяются ничем. Все четыре сверены вручную: ни одна не битая.
- **Видел носителя, класса не вывел:** `index.html:94` несёт зашитый `127.0.0.1:7391`,
  и никакой скрипт это поле не обновляет.
- Бегло или вовсе не читал: `check-artboard-labels.py`, генераторы фикстур, четыре
  набора тестов, `app.css`. **Пробел, названный пробелом.**
- **Дерево менялось под замером** — я правил `check-web.mjs` и `check-fixtures.py`
  прямо во время охоты; охотник переснял все номера строк грепом по новой редакции и
  сказал об этом.

## Что делаю первым

**Н3 — моя регрессия сегодняшнего вечера**, внесена вместе с тумблером `countdown`
около 20:30. Правило `BROWSER_ONLY` я сделал механизмом (до этого список не читал
никто), но фильтрует он по ЛИСТУ пути и ничего не пишет в `localStorage` — то есть
значение исчезает совсем. Это хуже, чем было: раньше настройка уезжала в конфиг и
хотя бы сохранялась.

**Н6 — одна строка** и он в моём же списке проверок: страж, который не может
покраснеть, стоит в единой команде и печатает зелёное.
